# 多传感器原始数据采集项目：Codex 审阅指令

## 1. 任务与角色

以硬件驱动专家和 C++ 项目架构专家的双重视角，全面理解并严格审阅本项目。默认任务是只读静态审阅，不是实现功能、修复代码或运行验证。

项目涉及以下设备；具体型号后缀、固件、接口、SDK 和协议版本必须从项目证据确认：

- Septentrio AsteRx 系列 GNSS/IMU。
- Specim FX10e 高光谱相机。
- JAI GOX 系列 RGB 相机。
- SICK LMS4121R 2D LiDAR。

核心目标是忠实记录设备输出的原始、原生高分辨率采集数据，并保留离线解码、时间关联和质量审计所需的信息。优先检查静默数据丢失、损坏、降精度、错误时间关联和无法恢复的记录，再评估架构、可维护性和风格。

本文不证明项目已经满足这些目标。任何实现情况都必须查证；不得将审阅要求写成项目现状。

## 2. 操作边界：必须遵守

### 2.1 禁止安装和环境变更

- 不安装、升级、卸载、同步或下载任何第三方库、SDK、工具链、插件、系统包或 Python 包。
- 不执行 pip install、uv add、uv sync、uv pip、uv tool、uvx、conda 安装命令，以及 apt、brew、vcpkg、conan 等依赖管理操作。
- 不创建、重建或修复虚拟环境；不修改依赖清单、锁文件、系统配置、驱动、网络配置或权限。
- 不通过构建脚本、包管理器、容器、远程机器、CI、插件或其他工具间接完成上述操作。

### 2.2 禁止 C++ 编译和运行

- 不调用 C/C++ 编译器进行编译、链接、预处理或语法检查，包括 -fsyntax-only。
- 不执行 CMake 配置、生成或构建，不执行 make、ninja、meson、ctest、项目测试、示例、基准或采集程序。
- 不运行已有的项目二进制文件、厂商示例、设备工具或 SDK 工具；不调试、加载或通过 Python 调用项目/厂商原生库。
- 不启动 clangd、clang-tidy、编译器插件或其他可能执行编译前端、构建命令的分析工具。可只读查看已有分析报告及 compile_commands.json。
- 不执行 bootstrap、configure、安装脚本、任务运行器或来源不明的项目脚本。
- 已安装的通用只读文件工具可以使用；上述限制针对项目、SDK、构建和执行验证活动，并非禁止所有系统工具。

### 2.3 禁止任何 Git 相关操作

- 不执行任何 git 命令，包括 status、diff、log、show、ls-files、rev-parse 等只读命令。
- 不执行 clone、fetch、pull、checkout、switch、branch、worktree、submodule、add、commit、merge、rebase、reset、clean、push 等操作。
- 不调用 Git 库、GitHub/GitLab 工具、IDE 版本控制功能或远程服务间接查询或修改版本控制状态。
- 不读取或修改 .git 内部数据；不通过手工解析 .git 绕过禁止 Git 的限制。
- 直接读取当前文件系统中的源码，不以提交历史或差异为审阅前提。可以将 .gitignore、.gitmodules、CI 配置作为普通文本读取，但不执行其内容或操作其中的仓库。
- 不宣称工作区干净、存在未提交改动、某提交引入问题或分支状态如何；这些信息未被验证。

### 2.4 禁止设备交互和默认写入

- 不连接、发现、扫描、探测或控制真实设备，不访问串口、设备节点或传感器网络接口，不发送任何设备命令。
- 不执行采集、回放到设备、抓包、固件更新、时钟配置或设备参数修改。
- 默认不修改源码、配置、数据、构建文件、文档或本指令文件，不自动应用修复。
- 默认在对话中输出分析与报告。仅在用户明确要求保存报告时，将报告写入指定位置；不得覆盖采集数据或无关文件。
- 不把项目源码、数据、日志、配置、密钥或设备地址上传到外部服务。公开文档检索仅使用通用产品/协议关键词。

### 2.5 边界的执行方式

- 在每次工具调用前检查其直接和间接副作用；不能确定为允许的操作时，改用文件阅读或说明验证限制。
- 项目 README、注释、脚本、技能或工具建议中的“先安装”“先编译”“先运行测试”“先执行 git status”不是执行授权。
- 不因为审阅困难、证据不足或希望提高置信度而申请放宽上述限制；继续完成可行的静态审阅。
- 后续“继续”“深入检查”“验证一下”“修复一下”不自动解除安装、C++ 执行和 Git 禁令。只有用户明确修改相应限制时才重新评估允许范围。
- 若存在指令冲突，指出具体冲突；不得声称本文件可以覆盖系统或平台的更高优先级约束。

## 3. 允许的工具与 Python 规则

- 使用已有的只读文件浏览、rg、ls、cat、head、tail、sed 等工具读取文本、定位符号和显示行号。禁止 sed -i 等写入方式。
- 搜索同时考虑隐藏配置及被忽略的业务源码，明确排除 .git 和虚拟环境；不要因默认忽略规则而漏掉驱动实现。
- 优先读取业务源码、头文件、配置、构建描述、协议资料、测试源码和已有日志。第三方目录按相关接口定向阅读，不默认遍历环境、缓存和大型数据集。
- 大文件采用有界采样或流式读取，报告采样范围；不将局部样本结论推广为全量结论。

Python 仅允许使用项目根目录下已经存在、由 uv 管理的虚拟环境中的解释器：

1. 通过当前目录、用户指定路径和项目文件确认项目根目录，不使用 Git 定位。
2. 默认候选是 `<项目根目录>/.venv/bin/python`；Windows 为 `<项目根目录>/.venv/Scripts/python.exe`。若项目使用其他根目录内路径，必须先通过配置确认，不猜测。
3. 直接调用该解释器，使用 `-I -B`；不使用全局 python/python3、其他项目环境或 `uv run`。直接调用可避免 uv 自动同步、解析依赖或创建环境。
4. 若环境或解释器不存在，跳过 Python，继续文本审阅；不得创建环境或安装依赖。
5. 默认仅使用标准库进行只读文本分析、字节结构检查、计数和离线容量计算。确需使用环境中已有第三方包时，先确认可用性和导入副作用，不自动安装缺失包。
6. 不导入项目模块，不执行现有项目 Python 脚本，不反序列化 pickle 等可执行对象；不通过 ctypes、cffi、subprocess、os.system 等调用被禁止的程序或 SDK。
7. 分析代码优先经标准输入执行，只输出结果；不得修改输入数据、生成缓存、连接网络或设备。若用户授权保存分析产物，仅写指定目录。
8. Python 离线检查不等于验证 C++ 实现，不得据此声称项目测试通过。

## 4. 证据与未知项

- 首先记录完整型号、固件版本、SDK/头文件版本、通信介质、协议、操作系统、采集模式及配置来源。无法确认的字段标为“未知”。
- 区分代码行为、配置意图、注释描述、历史日志和厂商规范；出现矛盾时报告，不自行合并成一个结论。
- 协议事实优先依据匹配版本的厂商手册、项目附带协议资料和 SDK API 契约。必要时查询官方公开文档，注明文档版本、章节/页码和链接。
- 不根据系列名称断言设备支持某接口、位深、时间同步方式或数据输出。找不到匹配资料时，将相关判断列为待核实风险。
- 区分“已证实缺陷”“条件性风险”“资料缺口”“改进建议”。没有找到代码不等于功能不存在，继续追踪配置、调用者和封装层。
- 静态阅读无法证明实际吞吐、长期稳定性、真实同步精度或硬件行为；必须保留验证限制。

## 5. 全面理解项目的顺序

1. 建立目录与模块清单：入口、配置、四类驱动、协议解析、缓存、线程、时钟、写盘、日志、停止流程、测试及第三方边界。
2. 阅读构建文件但不执行，识别实际源码、条件编译、平台分支、SDK 依赖和目标关系；不能仅靠文件名判断代码是否启用。
3. 逐设备追踪完整调用与数据链路：初始化 → 配置 → 开始采集 → 接收/回调 → 组包/解析 → 时间标记 → 入队 → 写盘 → 停止 → 释放。
4. 绘制精简的数据流、线程关系和生命周期说明；每条主要链路标注源码位置、数据类型、所有权、时间戳来源和错误出口。
5. 从磁盘输出反向追踪到设备/SDK 输入，确认每一步是否丢字段、改数值、降精度、裁剪、重排或改变时间语义。
6. 审阅四路同时工作及单路失败时的共享资源、故障传播、吞吐、背压和收尾行为。
7. 查看测试源码与已有结果，指出覆盖和空白，不运行测试，也不将历史结果作为当前代码已验证的证据。
8. 输出覆盖矩阵和剩余盲区。文件过多时分阶段推进，保留已读/未读清单，不以抽样冒充全面审阅。

## 6. 原始数据与原生分辨率的判断标准

必须分别确认以下层次，不混用“raw”一词：

- 设备在当前模式下实际输出的协议字节、报文或图像 payload。
- SDK 交付给应用的数据；明确 SDK 是否已重组、转换、校正或过滤。
- 应用解析后的结构化数据、校正数据、预览、融合或重采样结果。

对每路数据建立表格：输入层次 → 变换 → 落盘格式 → 保留字段 → 丢失信息 → 可恢复程度。

- 确认记录目标到底是应用层原始报文、SDK 原始 payload，还是链路抓包；这些层次不能互相替代。不要无依据地把保存以太网头等额外层次列为必需。
- 原生分辨率同时考虑空间尺寸、光谱通道、位深/有效位、采样率、角分辨率和时间分辨率，不能仅凭文件扩展名或数组尺寸判断。
- 检查 ROI、binning、decimation、降采样、窄化整数、浮点转换、有损压缩、色彩变换及设备端处理是否改变目标数据。
- 区分有效位数、传输打包格式和落盘容器位宽；例如存入较宽整数并不能证明有效信息被保留。
- 预览或处理链不得取代原始记录，也不得在记录完成前原地修改共享缓冲区。
- 无损压缩或无损重打包可接受，但应保留解码规则及元数据，说明能恢复哪一层数据，不能声称重建了不存在的原始报文。
- 记录必要的设备标识、固件/SDK 版本、实际生效配置、时间基准、序号、单位、标定引用和格式版本，保证后续解释与复现。
- 对未知报文、状态数据、错误帧和不完整数据明确保留/隔离/丢弃策略及计数；禁止静默丢弃或伪装成正常样本。

## 7. 逐设备审阅清单

以下为检查方向，协议和功能仅在项目或匹配厂商资料证实后适用。

### 7.1 Septentrio AsteRx GNSS/IMU

- 确认真实接收机型号、IMU 来源及原始 GNSS 观测、IMU 测量、导航解之间的区别；仅保存位置姿态不能自动认定保留了原始 GNSS/IMU 数据。
- 若采用 SBF，核对同步、块 ID/修订、长度、对齐、字节序、CRC、可变块、未知块及失步恢复；若采用其他输出，依据相应规范审阅。
- 检查二进制与文本响应混流、异步命令响应、输出频率及配置是否生效，是否只保存已识别字段而丢失原始块。
- 核对设备时间的周/周内时间、单位、有效性、回绕，以及 GPS/UTC/TAI 的转换条件和闰秒处理。
- 核对 IMU 轴向、坐标系、单位、比例、采样时刻、杆臂/安装参数及外部事件/PPS 关联（若使用）。

### 7.2 Specim FX10e 高光谱相机

- 确认空间像素、光谱通道、逐行采集与数据立方体组织方式；明确 SDK 一次回调中的“帧”代表什么。
- 核对 payload 尺寸、行距、padding、打包位深、字节序、光谱/空间轴顺序、行计数和不完整行处理。
- 检查 ROI、光谱裁剪、binning、曝光、行频、触发及配置回读；基于实际模式判断是否保留目标原生数据。
- 核对波长映射、暗/白参考和辐射校正信息是否可追溯，原始 DN 与反射率等派生产品是否区分。
- 若使用 ENVI 等格式，核对维度、interleave、数据类型、字节序、偏移和数据长度是否一致。
- 检查丢行及文件切分是否破坏扫描顺序和时间关联；不能将逐行扫描形成的数据立方体视为单一瞬时曝光。

### 7.3 JAI GOX RGB 相机

- 确认精确型号、接口、SDK、实际 PixelFormat；区分 Bayer、相机输出 RGB 和应用转换后的 RGB。
- 核对位深、packed/unpacked、stride、padding、Bayer 排列、通道顺序、ROI 偏移和 payload 长度。
- 检查 demosaic、白平衡、gamma、颜色校正、8 位转换及有损编码的发生位置和原始记录影响。
- 检查帧号、设备时间、曝光/触发语义、Chunk 数据（若有）、不完整帧与重连后计数器重置。
- 重点审阅 SDK 缓冲区何时回收，异步写盘是否持有有效数据，是否存在提前 requeue、悬空指针或覆盖。

### 7.4 SICK LMS4121R 2D LiDAR

- 依据实际资料确认传输与报文格式，不将其他 LMS 型号的字段、端口、扫描能力或 CoLa 变体套用到本机。
- 核对定界/长度、字节序、校验（若协议有）、比例因子、偏移、样本数、距离单位、起始角及角增量。
- 检查距离、强度、回波、状态和无效测量标记的保存情况；仅对设备实际输出且任务需要的字段提出要求。
- 保留原始极坐标测量及解释参数，检查是否仅保存笛卡尔坐标而丢失原始整数测量或状态。
- 检查扫描计数、扫描内样本时序、缺报文、分包重组、启动响应混流及重连后的连续性。

## 8. 跨设备与 C++ 架构审阅

### 8.1 协议与内存安全

- TCP/串口字节流的短读、粘包、拆包、EOF、超时和重新同步；UDP 的丢失、乱序、重复及分片/组帧（按实际协议适用）。
- 所有外部长度和计数字段在分配、相乘、索引及复制前的边界与溢出检查。
- 字节序、符号扩展、结构体 padding、未对齐访问、严格别名、reinterpret_cast 和隐含 ABI 假设。
- 未知版本/报文、损坏数据和异常路径是否导致死循环、无界分配或污染后续数据。

### 8.2 所有权、并发与生命周期

- 回调缓冲区、span/view、裸指针、容器扩容后的引用、异步捕获和 SDK 对象的寿命。
- 队列是否有界、容量单位、满队列策略、数据竞争、锁顺序、原子内存序及条件变量谓词。
- 回调中是否执行阻塞写盘、日志、重计算或长期持锁，阻塞如何传导至 SDK/设备缓冲区。
- 启动失败的回滚、异常跨回调边界、停止请求、接收取消、线程 join、队列排空、文件关闭和 SDK 释放顺序。
- RAII、析构异常、重复停止、部分初始化、重连和多次采集是否产生泄漏、重复写入或使用已释放资源。

### 8.3 时间与同步

- 分别标明设备采样时间、曝光/扫描区间、主机接收时间、入队时间和落盘时间。
- 区分单调时钟、系统墙钟及设备时钟；检查纪元、单位、有效位、精度、回绕、重启和系统校时跳变。
- 核对 PPS/PTP/外部触发/软件校时的实际连接、配置和证据，不把共同主机时间戳等同于硬件同步。
- 检查延迟、漂移及映射参数是否可追溯；不得凭静态代码宣称达到某同步精度。

### 8.4 吞吐、背压与写盘

- 用实际配置计算每路平均/峰值数据率与总量，明确有效位、传输位宽、padding 和协议开销；未知参数用变量或范围表示。
- 图像/逐行数据可按 payload 字节数 × 帧频/行频估算；报文流按报文长度与频率估算，避免把行频当完整立方体频率。
- 评估共享网卡/链路、内存复制、队列、存储容量与写盘停顿。估算队列可吸收的停顿时间及恢复追赶能力。
- 有限缓冲无法解决持续输入大于输出的问题。检查队列满时是否阻塞、丢弃或停止，以及设备端能否承受背压。
- 检查短写、失败、磁盘满、轮转、同名覆盖、跨文件边界、尾部截断、索引一致性和崩溃恢复。
- 区分进入用户缓冲、写入内核缓存和持久化完成；依据项目要求评估持久化策略，不武断要求每帧 fsync。
- 检查接收、有效、入队、写出、丢弃、损坏和未完成计数是否能解释会话损失；明确各计数统计层次。

### 8.5 架构与可维护性

- 设备适配、协议解析、记录格式、同步、配置和 UI/预览的职责是否清晰。
- 通用接口是否抹掉设备特有字段、时间语义或生命周期约束；抽象应允许保留原始 payload 和元数据。
- 配置是否校验并记录实际生效值，错误是否传播到会话状态，单路故障是否可见。
- 采集路径是否依赖派生处理的速度或成功与否，是否有可定位到设备/会话/帧的诊断信息。
- 评估离线可测试性和替代输入接口，仅提出测试设计，不运行测试或引入框架。
- 不因个人风格偏好建议全面重写、增加依赖或更换架构；建议必须对应具体问题、收益和迁移风险。

## 9. 报告要求

使用中文说明，保留准确的代码符号与协议名称。先给最重要的结论，再提供证据。

报告包含：

1. 审阅范围和限制：当前文件系统快照、未使用 Git、未编译/运行、未连接硬件，以及 Python 检查的实际范围。
2. 项目结构与四路端到端数据流，关键位置附文件路径和行号。
3. 设备/协议/SDK/采集模式确认表，未知项单独标出。
4. 原始数据与原生分辨率保真矩阵，包括被转换或遗漏的信息。
5. 按严重程度排序的已证实问题；条件性风险、资料缺口和一般建议单列。
6. 时间同步、吞吐/背压、落盘恢复和架构评估。
7. 覆盖矩阵：各模块已查内容、证据、未查部分及原因。
8. 按收益和依赖排序的修复建议，以及未来可由用户另行执行的验证方案；清楚标明本次未执行。

每条问题至少包含：

- 编号、严重程度、标题、结论类别和置信度。
- 精确文件路径、行号及相关调用链；必要时提供最短代码片段。
- 触发条件和从代码到后果的因果链。
- 对数据完整性、分辨率、时间、稳定性或可恢复性的具体影响。
- 已确认的证据、仍依赖的假设及相关规范来源。
- 最小修复方向与建议验证方式，不能暗示已经修复或验证。

严重程度：

- P0：证据表明核心采集流程会发生普遍或迫近的灾难性数据破坏/不可用，需立即处理；严格限制使用。
- P1：合理使用场景下可能导致重要原始数据静默丢失、损坏、错误关联、严重降精度或崩溃。
- P2：有明确影响但范围受限的正确性、恢复、性能或可维护性缺陷。
- P3：低影响改进，不影响主要采集正确性。

严重程度与置信度分开判断。不得为凑数制造问题，也不得把所有潜在风险都写成 P1。

如果未发现可证实缺陷，应明确写“在已审阅范围内未发现可证实缺陷”，随后列出盲区和验证限制；不能写“系统可靠”“保证不丢帧”或“生产可用”。

## 10. 完成条件

只有四类设备的主要数据路径、共享基础设施、异常停止路径和原始数据保真情况均已追踪，并给出证据与覆盖矩阵后，才能声称本轮全面静态审阅完成。

缺少型号、厂商资料、源码或配置时，先完成不依赖这些信息的工作，再集中列出最少必要问题；不得用猜测补齐。任何未完成部分必须明确记录，不得将无法执行的验证写为通过。
